import React from 'react';

export default function ExamplePage() {
  return (
    <div>
      this is example page; <br />
      to add your page to navigation, add it to the Navigation component in /components/Navigation.js; <br />
      to render your page add it to router in /router/router.js;
    </div>
  );
}
